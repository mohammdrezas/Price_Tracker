# سیستم رهگیری قیمت اکانت‌های بازی PS4 / PS5

> سند توضیح کامل پروژه — این فایل قبل از نوشتن هر خط کدی آماده شده تا هر کسی (کارفرما، هم‌تیمی، یا خودِ آینده‌مان) با خواندنش دقیقاً بفهمد این پروژه چیست، چه کاری قرار است انجام دهد، با چه ابزارهایی ساخته می‌شود و چطور قدم‌به‌قدم روی گیت‌هاب منتشر می‌شود.

---

## ۱. این پروژه چیست؟ (خلاصه در یک نگاه)

یک **سرویس بک‌اند (Background Service)** با **پایتون خالص** که هر روز قیمت اکانت‌های ظرفیتی بازی‌های PS4 و PS5 (مثل Ghost of Tsushima، Red Dead Redemption 2، EA Sports FC 25، Call of Duty) را از چند فروشگاه اینترنتی مختلف جمع‌آوری می‌کند، آن‌ها را در یک دیتابیس ذخیره می‌کند، تاریخچه‌ی تغییرات قیمت را نگه می‌دارد، و هر وقت قیمتِ یک بازی نسبت به میانگین اخیرش **افت قابل‌توجهی** کرد، یک **هشدار** می‌فرستد تا بهترین زمان خرید مشخص شود.

**این پروژه رابط کاربری (UI) ندارد.** یعنی نه سایت است و نه اپلیکیشن گرافیکی؛ کاملاً پشت‌صحنه کار می‌کند و خروجی‌اش داده و هشدار است.

> **مهم:** این پروژه **جنگو (Django) ندارد.** هدفش نمایش مهارت‌های پایتونِ خالص است. جنگو در یک پروژه‌ی جداگانه و بعد از رسیدن به سطح خوب کار می‌شود.

---

## ۲. چرا این پروژه؟ (هدف)

هدف اصلی، ساختن یک **نمونه‌کار واقعی و قابل‌ارائه در گیت‌هاب** است تا رزومه قوی‌تر شود. این پروژه به‌طور عملی این مهارت‌ها را نشان می‌دهد:

- **وب اسکرپینگ (Web Scraping):** استخراج داده از صفحات واقعی وب
- **برنامه‌نویسی نامتقارن (Async):** گرفتن هم‌زمان ده‌ها صفحه با `asyncio` و `aiohttp`
- **پاک‌سازی و پردازش داده (Data Cleaning):** تبدیل داده‌ی کثیف وب به داده‌ی تمیز و استاندارد
- **کار با دیتابیس و SQL:** طراحی جدول، ذخیره‌ی سری‌زمانی، و نوشتن کوئری‌های تحلیلی
- **منطق تجاری (Business Logic):** تشخیص خودکار افت قیمت و ارسال هشدار
- **مهندسی نرم‌افزار تمیز:** ساختار ماژولار، لاگ‌گیری، مدیریت خطا، تست، و مستندسازی

---

## ۳. سیستم چطور کار می‌کند؟ (جریان کلی)

مسیر داده در این سیستم به این شکل است:

```
[دیتابیس: لیست محصولات و لینک‌ها]
            │
            ▼
[اسکرپر: گرفتن صفحه‌ها  (اول ساده، بعد async)]
            │
            ▼
[پاک‌سازی: تبدیل "۲,۵۰۰,۰۰۰ تومان" → 2500000]
            │
            ▼
[ذخیره‌ی سری‌زمانی: هر قیمت با تاریخ و زمان دقیق]
            │
            ▼
[تحلیل با SQL: مقایسه با میانگین هفته‌ی گذشته]
            │
            ▼
[هشدار: اول ترمینال/لاگ، بعد ربات تلگرام]
```

### مرحله به مرحله‌ی عملکرد سیستم

**۱) پیکربندی اهداف در دیتابیس**
در دیتابیس یک جدول `products` می‌سازیم و لینک صفحه‌ی هر بازی در هر فروشگاه را در آن ذخیره می‌کنیم. سیستم برای شروع، این لیست را از دیتابیس می‌خواند.

**۲) استخراج داده**
سیستم به سراغ لینک‌ها می‌رود و HTML صفحه‌ها را می‌گیرد. با `BeautifulSoup` قیمت و وضعیت موجودی را از داخل HTML بیرون می‌کشد.
> در ابتدا این کار را **ساده و پشت‌سرهم** با `requests` انجام می‌دهیم تا مطمئن شویم کل مسیر درست کار می‌کند؛ سپس در یک مرحله‌ی جداگانه آن را به حالت **نامتقارن (async)** با `asyncio` + `aiohttp` بازنویسی می‌کنیم تا ده‌ها لینک هم‌زمان گرفته شوند.

**۳) پاک‌سازی داده‌ها**
داده‌های خام وب کثیف‌اند (مثلاً «۲,۵۰۰,۰۰۰ تومان» یا «ناموجود»). در این مرحله کاماها، حروف و کاراکترهای اضافی را حذف می‌کنیم، ارقام فارسی را به انگلیسی تبدیل می‌کنیم، و قیمت را به عدد صحیح استاندارد (Integer) تبدیل می‌کنیم.

**۴) ذخیره‌سازی سری‌زمانی**
داده‌ی تمیزشده را در جدول `price_history` ذخیره می‌کنیم. برای هر قیمت یک **Timestamp** (تاریخ و زمان دقیق) ثبت می‌شود. این کار اجازه می‌دهد بعداً با کوئری SQL روند قیمت هر بازی را در بازه‌ی زمانی دلخواه تحلیل کنیم.

**۵) تحلیل و هشدار خودکار**
یک بخش جداگانه هر روز دیتابیس را چک می‌کند. اگر کوئری‌های SQL نشان دهند قیمت یک بازی نسبت به میانگین هفته‌ی گذشته افت شدیدی داشته (یعنی تخفیف خورده)، سیستم یک هشدار می‌فرستد.
> در ابتدا هشدار فقط در **ترمینال و فایل لاگ** نمایش داده می‌شود؛ سپس در مرحله‌ی بعد یک **ربات تلگرام** اضافه می‌کنیم که پیام را مستقیم بفرستد.

---

## ۴. تصمیم‌های فنی (چه ابزارهایی و چرا)

| موضوع | انتخاب | دلیل |
|-------|--------|------|
| زبان | **Python** (خالص، بدون فریم‌ورک وب) | تمرکز روی مهارت پایتون |
| دیتابیس | **SQLite** | داخل خود پایتون است، صفر پیکربندی، بدون نصب؛ برای شروع عالی و بعداً قابل مهاجرت به PostgreSQL |
| کار با دیتابیس | **SQL خام با کتابخانه‌ی `sqlite3`** | مهارت SQL مستقیم در کد دیده می‌شود (هماهنگ با سابقه‌ی Oracle BI) |
| گرفتن صفحه | اول `requests`، بعد `aiohttp` + `asyncio` | اول ساده و قابل‌فهم، بعد بهینه و حرفه‌ای |
| استخراج از HTML | `BeautifulSoup` (کتابخانه‌ی `bs4`) | استاندارد و ساده برای پارس HTML |
| هشدار | اول **ترمینال / لاگ**، بعد **ربات تلگرام** | مسیر یادگیری ساده‌تر |
| زمان‌بندی اجرا | **cron** (یا Task Scheduler ویندوز) | اجرای خودکار روزانه |
| مدیریت اطلاعات حساس | فایل `.env` | توکن تلگرام و... داخل کد و گیت‌هاب قرار نگیرد |
| لاگ‌گیری | ماژول `logging` پایتون | به‌جای `print`، نشانه‌ی کد حرفه‌ای |
| تست | `pytest` | چند تست ساده، خصوصاً برای بخش پاک‌سازی داده |

---

## ۵. تغییرات نسبت به ایده‌ی اولیه

این‌ها تصمیم‌هایی هستند که برای بهتر شدن مسیرِ یادگیری و کیفیت پروژه گرفته‌ایم:

1. **حذف کامل جنگو از این پروژه** — این پروژه پایتون خالص است؛ جنگو جداگانه کار می‌شود.
2. **async بعد از نسخه‌ی ساده اضافه می‌شود، نه از اول** — اول کل مسیر را با یک سایت و به‌صورت ساده کامل می‌کنیم، بعد آن را به async بازنویسی می‌کنیم. این هم یادگیری را ساده‌تر می‌کند و هم تاریخچه‌ی گیت‌هاب داستان قشنگی پیدا می‌کند («ساختمش، بعد بهینه‌اش کردم»).
3. **شروع با یک فروشگاه، بعد گسترش به چند فروشگاه** — نه ۱۰ سایت از روز اول.
4. **هشدار اول در ترمینال/لاگ، بعد تلگرام** — به‌جای شروع مستقیم با ربات تلگرام.
5. **اضافه شدن موارد «حرفه‌ای دیده‌شدن»:** ساختار ماژولار، `README`، `.gitignore`, `.env`، `logging`، مدیریت خطا و retry، رعایت ادب اسکرپ (User-Agent مناسب و فاصله بین درخواست‌ها)، و چند تست با `pytest`.

---

## ۶. ساختار پیشنهادی پروژه

```
Claude_Project/
├── README.md                # معرفی پروژه (نسخه‌ی نهایی برای گیت‌هاب)
├── PROJECT_OVERVIEW.md      # همین سند
├── requirements.txt         # لیست کتابخانه‌های موردنیاز
├── .gitignore               # فایل‌هایی که نباید در گیت‌هاب بروند
├── .env.example             # نمونه‌ی فایل تنظیمات (بدون مقادیر واقعی)
├── config.py                # خواندن تنظیمات و مسیرها
│
├── database/
│   ├── schema.sql           # ساختار جدول‌ها (products, price_history)
│   └── db.py                # اتصال به دیتابیس و توابع ذخیره/خواندن
│
├── scraper/
│   ├── fetch.py             # گرفتن صفحه (نسخه‌ی ساده و بعداً async)
│   └── parse.py             # استخراج قیمت و موجودی از HTML
│
├── processing/
│   └── clean.py             # پاک‌سازی و استانداردسازی داده
│
├── analysis/
│   └── analyze.py           # کوئری‌های SQL برای تشخیص افت قیمت
│
├── notifier/
│   └── notify.py            # ارسال هشدار (ترمینال/لاگ و بعداً تلگرام)
│
├── tests/
│   └── test_clean.py        # تست بخش پاک‌سازی داده
│
└── main.py                  # نقطه‌ی شروع؛ همه‌ی مراحل را به هم وصل می‌کند
```

> این ساختار ممکن است حین کار کمی تغییر کند؛ ولی ایده‌ی اصلی این است که هر بخش کارِ خودش را در ماژول جدای خودش انجام دهد، نه اینکه همه‌چیز در یک فایل بزرگ باشد.

---

## ۷. جدول‌های دیتابیس

**جدول `products` — لیست محصولاتی که رهگیری می‌شوند:**

| ستون | نوع | توضیح |
|------|-----|-------|
| `id` | INTEGER | شناسه‌ی یکتا |
| `name` | TEXT | نام بازی (مثلاً Red Dead Redemption 2) |
| `platform` | TEXT | PS4 یا PS5 |
| `store` | TEXT | نام فروشگاه |
| `url` | TEXT | لینک صفحه‌ی محصول |
| `is_active` | INTEGER | آیا این محصول فعلاً رهگیری شود یا نه |

**جدول `price_history` — تاریخچه‌ی قیمت (سری‌زمانی):**

| ستون | نوع | توضیح |
|------|-----|-------|
| `id` | INTEGER | شناسه‌ی یکتا |
| `product_id` | INTEGER | ارجاع به `products.id` |
| `price` | INTEGER | قیمت تمیزشده (عدد صحیح) |
| `in_stock` | INTEGER | موجود بودن (۱) یا نبودن (۰) |
| `checked_at` | TIMESTAMP | تاریخ و زمان دقیق ثبت قیمت |

---

## ۸. چطور قدم‌به‌قدم روی گیت‌هاب منتشر می‌شود

روش کار ما این است که پروژه را **تکه‌تکه و مرحله‌به‌مرحله** پیش می‌بریم و **هر مرحله را با یک کامیت (commit) جداگانه و معنادار** روی گیت‌هاب می‌گذاریم. این باعث می‌شود:

- تاریخچه‌ی گیت‌هاب یک **مسیر رشد واقعی** را نشان دهد، نه یک آپلود یک‌باره‌ی همه‌چیز.
- هر کسی که پروفایل را می‌بیند بفهمد **چطور فکر کرده‌ایم و چطور پروژه را ساخته‌ایم**.
- اگر جایی اشتباه شد، راحت بشود به عقب برگشت.

اصول کاری ما هنگام کار با گیت:

- هر مرحله که کامل و کارکردنی شد → یک کامیت با پیام واضح (مثلاً `Add products and price_history tables`).
- **هیچ‌وقت** فایل‌های حساس (مثل `.env` با توکن واقعی) و فایل‌های زائد (venv، دیتابیس محلی، کش‌ها) داخل گیت نمی‌روند — این‌ها در `.gitignore` قرار می‌گیرند.
- `README.md` به‌مرور کامل‌تر می‌شود تا در پایان، یک معرفی حرفه‌ای همراه با توضیح و نمونه‌ی خروجی باشد.
- کار را **باهم و قدم‌به‌قدم** پیش می‌بریم؛ من راهنمایی و توضیح می‌دهم و بخش‌ها را می‌نویسیم، ولی هدف این است که خودت هم بفهمی و یاد بگیری — نه اینکه فقط کد تحویل بگیری.

---

## ۹. چیزهایی که هنوز باید مشخص شوند

برای شروعِ کدنویسی، این دو مورد لازم است:

1. **سایت فروشگاه هدف اول:** باید بدانیم اولین فروشگاهی که رهگیری می‌کنیم کدام است، چون تعیین می‌کند اسکرپ ساده است یا پیچیده:
   - اگر قیمت در **HTML ساده** باشد → با `requests` + `BeautifulSoup` راحت کار می‌کنیم.
   - اگر قیمت با **جاوااسکریپت** لود شود → به ابزار سنگین‌تری مثل Selenium/Playwright نیاز داریم.
2. **آماده بودن گیت و گیت‌هاب:** نصب بودن `git` روی سیستم و داشتن اکانت گیت‌هاب برای انتشار.

---

## ۱۰. جمع‌بندی

این پروژه یک سرویس بک‌اند پایتونی است که به‌صورت خودکار قیمت اکانت‌های بازی را از فروشگاه‌های مختلف جمع می‌کند، تاریخچه‌شان را نگه می‌دارد، افت قیمت را تشخیص می‌دهد و هشدار می‌فرستد. بدون رابط کاربری، بدون جنگو، و با تمرکز کامل روی مهارت‌های واقعیِ پایتون: اسکرپینگ، async، پاک‌سازی داده، SQL، و مهندسی نرم‌افزار تمیز. کل کار **قدم‌به‌قدم و با کامیت‌های معنادار روی گیت‌هاب** منتشر می‌شود تا هم یک نمونه‌کار قوی برای رزومه بسازد و هم یک مسیر یادگیری واقعی را نشان دهد.
